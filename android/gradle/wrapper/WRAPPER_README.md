# Gradle Wrapper

The gradle-wrapper.jar file should be generated automatically when you open this project in Android Studio.

Alternatively, if you have Gradle installed locally, run:
```bash
gradle wrapper --gradle-version 8.2
```

The wrapper JAR file (~60KB) is intentionally excluded from git but will be auto-generated on project sync.
